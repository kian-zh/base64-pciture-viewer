{
  "name": "base64-picture-viewer",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "homepage": "https://kian-zh.github.io/base64-picture-viewer",
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@rsbuild/core": "^1.0.1",
    "@rsbuild/plugin-react": "^1.0.1",
    "gh-pages": "^4.0.0",
    "@types/react": "^18.2.0",
    "@types/react-dom": "^18.2.0"
  },
  "scripts": {
    "dev": "npx rsbuild dev --open false",
    "start": "npx rsbuild dev --open false",
    "start:open": "npx rsbuild dev",
    "build": "npx rsbuild build",
    "preview": "npx rsbuild preview",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  }
}
